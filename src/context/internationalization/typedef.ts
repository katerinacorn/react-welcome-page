export enum Lang {
    EN = "en",
    UA = "ua"

}

export type Dictionary = {
    navigation: {
        home: string,
        about: string,
        contacts: string,
        news: string,
    };

    content: {
        header: string,
    };
}